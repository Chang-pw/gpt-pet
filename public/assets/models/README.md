Place your production-ready `.glb` desktop pet model in this folder.

Default expected path:

- `public/assets/models/desktop-pet.glb`

Recommended animation clips:

- `Idle`
- `Walk`
- `Wave` or `Happy`
- `Sleep`

If no model is present, the app will render a procedural fallback pet so the MVP can still run.
